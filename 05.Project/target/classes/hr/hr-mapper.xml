<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hr">
  <select id="emplist" resultType="hr.EmployeeDTO">
     SELECT      *
 	 FROM       employees
  </select>

  <select id="deptlist" resultType="hr.DeptDTO">
     SELECT      *
 	 FROM        departments
  </select>
 
  
</mapper>