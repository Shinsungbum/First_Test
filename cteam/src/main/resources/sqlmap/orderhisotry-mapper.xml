<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="orderhistory">

<!-- 주문내역 리스트 jk -->
<select id="list">
select mi.menu_name, mi.menu_image, si.store_name, oh.menu_cnt, oh.price, oh.*  from menu_info mi left outer join store_info si
on mi.store_code = si.store_code
left outer join order_history oh 
on oh.store_code = si.store_code
where oh.id = #{id}
</select>

</mapper>